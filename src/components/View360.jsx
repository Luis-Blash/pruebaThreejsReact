import React, { useEffect, useRef } from 'react'
import { AppThree } from '../utils/Three/App';

export const View360 = () => {
    const container = useRef(null)
    useEffect(() => {
        
    }, [])
    return (
        <div ref={container}/>
    )
}
